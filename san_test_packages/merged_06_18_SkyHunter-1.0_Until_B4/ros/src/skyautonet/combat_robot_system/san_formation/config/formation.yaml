formation_node:
  ros__parameters:
    initial_formation_id:    3       # V-Shape
    initial_preset_id:       2       # recon_defence (d=5m, θ=90°)
    realign_threshold_m:     2.0     # KPP-1 — re-plan if avg err > 2m
    max_speed_recon_mps:     1.3
    lead_bias_s:             0.1
    # DCN-2026-026 C-2 — Encircle combat (ratified 2026-06-10)
    encircle_radius_m:       7.0
    encircle_auto:           false   # 운용자 확인 기본 — 자동 개시는 opt-in
    encircle_min_confidence: 0.9
    encircle_ttl_s:          10.0
    encircle_reentry_s:      5.0
